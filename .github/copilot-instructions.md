# Copilot Instructions

- 优先遵循现有代码风格与目录结构；最小化改动范围。
- 新增/修改接口时同时更新对应的使用方与文档（如 README、注释）。
- 避免引入不必要依赖；如必须引入，说明原因并锁定版本。
- 输出确定性结果：排序、格式化、字段命名保持一致。
- 数据文件（如 JSON）保持 UTF-8 编码与稳定字段结构；避免写入运行时生成字段。
- 出错时返回清晰错误信息（含上下文），不要静默失败。
